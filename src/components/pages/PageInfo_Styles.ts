import styled from "styled-components";

const PageInfo = styled.section`
  padding: 35px 0;
`

const Description = styled.div`
  font-weight: 400;
  font-size: 16px;
  color: #fff;
`

export const S = {
    PageInfo,
    Description
}